import { shim } from '../shims/Shim';

/**
 * Loads variable from window with the
 * name 'MarkerClusterer'
 * 
 * @export
 * @returns {*} window['MarkerClusterer']
 */
export let MarkerClusterer = shim('MarkerClusterer'); 